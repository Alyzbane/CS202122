#ifndef LINE_H
#define LINE_H
#include <stdio.h>

void clear_line(void);
void add_words(const char *word);
int space_remaining(void);
void write_line(void);
void flush_line(void);
#define PRINT_LINE(a,b) printf("\n" #a " = %d\t"#b"= %d\n", a, b)

#endif
